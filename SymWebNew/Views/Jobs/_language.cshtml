@model IEnumerable<SymViewModel.Common.ApplicantLanguageVM>
@using UIHelper.MVC
@{
    ViewBag.Title = "_language";
}
<style>
    .details {
        grid-template-columns: 1fr 1fr 2fr 1fr 1fr 1fr 1fr 1fr;
        display: grid;
        gap: 2px;
        margin: 5px 0;
    }

    .qualifil input {
        font-size: 14px;
        font-weight: 400;
    }
</style>
<div class="detailpq">
    @using (Html.BeginCollectionItem("ApplicantLanguageVM"))
    {
        <section class="career-objectiveemp">
            <h2>Language Proficiency</h2>
            <table class="career-table">
                <thead>
                    <tr>
                        <th>Actions</th>
                        <th>Language </th>
                        <th>Reading </th>
                        <th>Writing </th>
                        <th>Speaking </th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <a href="@Url.Action("DeleteLanguage", "JobCircular", new { Id = item.Id, ApplicantId = item.ApplicantId })">
                                        <span class="sym-btn-remove-row"></span>
                                    </a>
                                </td>
                                <td>@Html.DisplayFor(m => item.Language)</td>
                                <td>@Html.DisplayFor(m => item.Reading)</td>
                                <td>@Html.DisplayFor(m => item.Writing)</td>
                                <td>@Html.DisplayFor(m => item.Speaking)</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" style="text-align: center;">No records found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </section>
    }
</div>
<script>
    $(function () {

    });
    $(document).ready(function () {

    });
</script>
