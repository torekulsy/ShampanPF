@model IEnumerable<SymViewModel.Common.ApplicantTrainingVM>
@using UIHelper.MVC
@{
    ViewBag.Title = "_applicantTraining";
}
<style>
    .details {
        grid-template-columns: 1fr 1fr 2fr 1fr 1fr 1fr 1fr 1fr;
        display: grid;
        gap: 2px;
        margin: 5px 0;
    }

    .qualifil input {
        font-size: 14px;
        font-weight: 400;
    }
</style>
<div class="detailpq">
    @using (Html.BeginCollectionItem("ApplicantTrainingVM"))
    {
        <section class="career-objectiveemp">
            <h2>Training Summery</h2>
            <table class="career-table">
                <thead>
                    <tr>
                        <th>Actions</th>
                        <th>TrainingTitle</th>
                        <th>Topic</th>
                        <th>Institute</th>
                        <th>Country</th>
                        <th>Location</th>
                        <th>Year</th>
                        <th>Duration</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <a href="@Url.Action("DeleteTraining", "JobCircular", new { Id = item.Id, ApplicantId = item.ApplicantId })">
                                        <span class="sym-btn-remove-row"></span>
                                    </a>
                                </td>
                                <td>@Html.DisplayFor(m => item.TrainingTitle)</td>
                                <td>@Html.DisplayFor(m => item.Topic)</td>
                                <td>@Html.DisplayFor(m => item.Institute)</td>
                                <td>@Html.DisplayFor(m => item.Country)</td>
                                <td>@Html.DisplayFor(m => item.Location)</td>
                                <td>@Html.DisplayFor(m => item.Year)</td>
                                <td>@Html.DisplayFor(m => item.Duration)</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" style="text-align: center;">No records found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </section>
    }
</div>
<script>
    $(function () {

    });
    $(document).ready(function () {

    });
</script>
